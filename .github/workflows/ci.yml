name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  verify:
    name: Verify Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        python-version:
          - "3.11"
          - "3.12"
          - "3.13"

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Show Python version
        run: python3 --version

      - name: Install package in editable mode
        run: python3 -m pip install -e .

      - name: Run unit tests
        run: PYTHONPATH=src python3 -m unittest discover -s tests

      - name: Compile-check source, scripts, and tests
        run: python3 -m compileall src scripts tests

      - name: Run release check
        run: sh scripts/release_check.sh

  verify-all-extras:
    name: Verify agentcapsule[all] Python 3.13
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip

      - name: Install package with all optional extras
        run: python3 -m pip install -e ".[all]"

      - name: Run full unit tests with all extras
        run: PYTHONPATH=src python3 -m unittest discover -s tests

      - name: Run release check with all extras smoke path
        run: sh scripts/release_check.sh
