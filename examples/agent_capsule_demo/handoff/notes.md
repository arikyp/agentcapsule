# Agent Capsule Demo Handoff

This folder is a tiny deterministic handoff bundle for the V0 demo script.

The receiver should inspect the capsule metadata, verify the SHA256, and unpack
the files into a local output directory before using them.
